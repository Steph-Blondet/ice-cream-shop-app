<!DOCTYPE html>
<html>
    <head>
        <meta charset="utf-8">
        <title>Edit User Profile</title>
        <!-- css stylesheet link -->
        <link rel="stylesheet" type="text/css" href="/style.css">
        <!-- google fonts link -->
        <link href="https://fonts.googleapis.com/css?family=Muli:400,800|Pacifico" rel="stylesheet">
        <!-- viewport -->
        <meta name="viewport" content="width=device-width,initial-scale=1">
    </head>
    <body>
        <header>
            <div class="full-navbar">
                <div class="half-navbar-left">
                    <%- include ../partials/navbar.ejs %>
                </div>
            </div>
        </header>

        <main>
            <h1>Let's update your profile <%=user.username%></h1>

            <a href="/users/<%=user._id%>">Back to my profile</a>

            <form action="/users/<%=user._id%>?_method=PUT" method="POST">
                <input type="text" name="username" value="<%=user.username%>"/><br/>
                <input type="text" name="password" value="<%=user.password%>"/><br/>
                <input type="submit" value="Update User"/>
            </form>
        </main>

        <!-- including ejs template for footer -->
        <%- include ../partials/footer.ejs %>

    </body>
</html>
