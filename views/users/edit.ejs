<!DOCTYPE html>
<html>
    <head>
        <meta charset="utf-8">
        <title>Edit User Profile</title>
        <!-- css stylesheet link -->
        <link rel="stylesheet" type="text/css" href="/style.css">
        <!-- google fonts link -->
        <link href="https://fonts.googleapis.com/css?family=Muli:400,800|Pacifico" rel="stylesheet">
        <!-- viewport -->
        <meta name="viewport" content="width=device-width,initial-scale=1">
    </head>
    <body class="register-page">
        <header>
            <div class="full-navbar">
                <div class="half-navbar-left">
                    <%- include ../partials/navbar.ejs %>
                </div>
            </div>
        </header>

        <main>
            <h1 class="tittle">Let's update your profile <%=user.username%></h1>



            <div class="form-container">
                <p class="page-link" id="edit-user"><a href="/users/<%=user._id%>">Back to my profile</a></p>

                <form action="/users/<%=user._id%>?_method=PUT" method="POST">
                    <input type="text" name="username" value="<%=user.username%>"/><br/>
                    <input type="password" name="password" value="<%=user.password%>"/><br/>
                    <input class="button" type="submit" value="Update User"/>
                </form>
            </div>

        </main>

        <!-- including ejs template for footer -->
        <%- include ../partials/footer.ejs %>

    </body>
</html>
